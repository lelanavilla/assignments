 const = {
     name: 'John',
     age: 101,
     pets: ["cat", "dog"]
 },

 const petObjects = []

 for (let i = 0; i < pets.length; i++) {
     let pet = pets[i]
 }
 if (pets[i] === "cat") {
     name = { fluffy };
 } else {
     name = { spot };
 }
 pet.name = name
 petObjects.push(pet.name);

 console.log(petObjects);