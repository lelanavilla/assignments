var ask = require("readline-sync");

var goombas = form.elements("baddies").getElementById("goombas";)
var bob-ombs= form.elements("baddies").getElementById("bob-ombs");
var cheep = form.elements("baddies").getElementById("cheep");

function(n)
var totalofBaddies="";
  function readNum (form){
      testVar = form.input.value;
      parseInt.testVar;
  
  }