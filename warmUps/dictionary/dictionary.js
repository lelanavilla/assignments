var dict = {
    var words = {
        startsWith: "Q"
        var qWords = {
            word: "quack",
            def: "the harsh sound of a duck"
        }
        word: "quadrilateral",
        def: "a four-sided polygon, like a square, rectangle, or rhombus."
    }
    word: " quadruped",
    def: "an animal especially a mammal having four limbs",
}


// function to do search of dictionary for a word
function searchDict(dict) {
    var search = dict.map(function(words) {
        for (var word in dict) {
            var value = dict.words;
        }
    });


}
//BENS WAY
// var dict = {};

// function lookItIp(dict, word, def) {
//     if (dict.hasOwnProperty(word)) {
//         return dict[word];
//     }else {
//         dict[word]=def;
//         //BOBS WAY
//       //  dict[word] = undefined;
//     }
// }
//function to look up word starting with a specific letter
function whichLetter(dict, words) {
    var letterStartsWith = dict.words.startsWith.value;
}
//function to find a word under the letter "q"
function findQWord(qWords) {
    whichLetter.letterStartsWith = "Q";
}









//create a function so a user can add a word the dictionary

var addWord